# cam-farm — placeholder (Run 9)

Will contain the redroid/v4l2loopback provisioning pipeline:

- `provision.py` — persona → container → verify → snapshot
- `loopback.sh` — v4l2loopback + ffmpeg feed setup
- `hal_config/` — persona camera characteristics baked into HAL configs

Nothing here yet; see BUILD_BIBLE.md Run 9.
